import { TypeLayout } from "../common/typeLayout";

export function ImpactMethodView(p: { data: any; json: any }) {
    const { data } = p;
    return (
      <TypeLayout
        map={{
            
        }}
        data={data}
      />
    );
  }
  